{
  "name": "toolhub",
  "version": "2.0.0",
  "private": true,
  "description": "A curated bilingual directory of useful browser tools.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "check": "node scripts/check-site.mjs",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.110.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
